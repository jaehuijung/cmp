<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper 
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sl.qr.mh.service.old.databaseMapper">

    <!-- [추가] 로그 등록 > id/ip/time/QR일련번호 -->
    <insert id="insertLogParamQR" parameterType="hashMap">
        insert into qrlog(id, ip, times, RackNumber, RackLocation, ServerName, PortNumber)
        values( #{id},
                #{ip}, 
                #{times},
                #{RackNumber},
                #{RackLocation},
                #{ServerName},
                #{PortNumber} )
    </insert>

</mapper>